import base64

encoded = ""
decoded_bytes = base64.b64decode(encoded)

key = "????????????"  

plaintext = ''.join(chr(b ^ ord(key[i % len(key)])) for i, b in enumerate(decoded_bytes))

print("Hasilnya:", plaintext)
